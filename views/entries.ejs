<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> </title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="stylesheet" href="/css/index.css">
</head>
<body>
    <header><%- include ('./partials/header') %></header>
    <main>
      <div class="container">
        <h1>Посты</h1>
      </div>
      <div class="container">
      <%#- include ('./partials/messages') %>
      <% entries.forEach((entry)=>{ %>
          <div class="card-entry" style="width: 66rem; padding: 10px;">
              <div class="card-body ">
                  <h5 class="card-title">Тема: <%= entry.title %></h5>
                  <p class="card-text">
                      Сообщение: <%= entry.content %> 
                  </p>
                  </br>
                  <h6>Автор:   <%= entry.username %> </h6> 
                  <p class="card-text"><small class="text-body-secondary"><%= entry.created_at %></small></p>               
                  <% if (locals.user) { %>
                      <% if (locals.admin || entry.username === locals.user.name) { %>
                        <a type="button" href="/update/<%= entry.id %>" class="btn btn-primary me-2 btn-update" style="border: 2px #c9007c solid; color: #ffffff; background-color: #c9007c;">
                          Изменить
                        </a>
                        <% if (locals.admin || entry.username === locals.user.name) { %>
                          <a type="button" href="/delete/<%= entry.id %>" class="btn btn-primary me-2 btn-delete" style="border: 2px #c9007c solid; color: #ffffff; background-color: #c9007c;">
                            Удалить
                          </a>
                        <% } %>
                      <% } %>
                    <% } %>
                  
              </div>
          </div> 
          </br>
      <% }) %> 
      </div>
    </main>
    <footer><%- include ('./partials/footer') %></footer>
</body>
</html>